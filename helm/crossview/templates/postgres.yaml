{{- if .Values.database.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "crossview.fullname" . }}-postgres
  namespace: {{ include "crossview.namespace" . }}
  labels:
    {{- include "crossview.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "crossview.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: postgres
  template:
    metadata:
      annotations:
        {{- with .Values.database.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "crossview.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: postgres
        {{- with .Values.database.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.database.securityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: postgres
          image: "{{ .Values.database.image.repository }}:{{ .Values.database.image.tag }}"
          {{- with .Values.database.containerSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: postgres
              containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: {{ .Values.config.database.database | quote }}
            - name: POSTGRES_USER
              value: {{ .Values.config.database.username | quote }}
            {{- $dbPassSecret := .Values.secrets.dbPassword | default dict }}
            {{- $dbPasswordValueFrom := include "crossview.secretValueFrom" (dict "secret" $dbPassSecret "secretName" (printf "%s-secrets" (include "crossview.fullname" .)) "secretKey" "dbpassword") }}
            {{- if $dbPasswordValueFrom }}
            - name: POSTGRES_PASSWORD
              {{- $dbPasswordValueFrom | nindent 14 }}
            {{- end }}
            {{- if .Values.database.persistence.enabled }}
            - name: PGDATA
              value: /var/lib/postgresql/data/crossview
            {{- end }}
            {{- with .Values.database.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          volumeMounts:
            {{- if .Values.database.persistence.enabled }}
            - name: postgres-storage
              mountPath: /var/lib/postgresql
            {{- end }}
            - name: postgres-socket
              mountPath: /var/run/postgresql
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pg_isready -U "{{ .Values.config.database.username }}"
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pg_isready -U "{{ .Values.config.database.username }}"
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            {{- toYaml .Values.database.resources | nindent 12 }}
      volumes:
        {{- if .Values.database.persistence.enabled }}
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: {{ include "crossview.fullname" . }}-postgres-pvc
        {{- end }}
        - name: postgres-socket
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "crossview.fullname" . }}-postgres
  namespace: {{ include "crossview.namespace" . }}
  labels:
    {{- include "crossview.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
  {{- if and .Values.database.service (hasKey .Values.database.service "annotations") }}
  annotations:
    {{- toYaml .Values.database.service.annotations | nindent 4 }}
  {{- end }}
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    {{- include "crossview.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
{{- if .Values.database.persistence.enabled }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "crossview.fullname" . }}-postgres-pvc
  namespace: {{ include "crossview.namespace" . }}
spec:
  accessModes:
    - {{ .Values.database.persistence.accessMode }}
  {{- if .Values.database.persistence.storageClass }}
  storageClassName: {{ .Values.database.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.database.persistence.size }}
{{- end }}
{{- end }}